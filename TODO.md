# TODO List for the Single-Post Blogging Platform

- [x] Create the Go HTTP server to serve `plan.md`.
- [x] Implement Markdown to HTML conversion within the Go server.
- [x] Refactor rendering logic into a shared internal package for use by both Server and CLI.
- [x] Implement the `plan` CLI tool structure (`cmd/plan`).
- [x] Implement `plan preview` command (start server locally, open browser).
- [x] Implement `plan publish` command (git add, commit, push).
- [x] Design a basic, minimalist HTML template for rendering the post.
- [x] Document usage and setup in `README.md`.
- [x] Write unit tests for the Markdown conversion and server.
- [ ] Add basic CSS for readability.
- [ ] Implement `post.md` as a placeholder markdown file.
- [ ] Write unit tests for the Markdown conversion and server.
- [ ] Document usage and setup in `README.md`.
